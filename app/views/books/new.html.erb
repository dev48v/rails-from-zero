<%# STEP 4 — new view: empty form for adding a book by hand. %>
<%# The real Open Library import lands in Step 7; this is the manual fallback. %>
<h1 class="h3 mb-4">Add a book</h1>
<%= render "form", book: @book %>
